# KubePing 1.1.0

Kubeping 1.1.0 introduces a redesigned web interface with a cleaner layout, and several tweaks improving overall usability. Deployment has also been enhanced with more flexible Helm configuration.

❗**Breaking change**: Prometheus metric name has been updated from `probe_result` to `kubeping_probe_result`. Update your dashboards and alerts accordingly.